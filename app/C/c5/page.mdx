import QuizQuestion from '../../../components/QuizQuestion';

# Instrukcje warunkowe

## Wprowadzenie

Instrukcje warunkowe pozwalają wykonywać **różne fragmenty programu** w
zależności od spełnienia określonych warunków. Dzięki nim program może
podejmować decyzje — reagować na dane, wyniki obliczeń czy działania
użytkownika.

W języku C mamy kilka sposobów podejmowania decyzji:

- `if`
- `else`
- `else if`
- operator trójargumentowy `?:`
- `switch` (rozgałęzienia wielokrotne)

---

## Jak działają warunki?

Warunek to wyrażenie, które **zwraca wartość logiczną**:

- `0` → **fałsz**
- dowolna wartość **niezerowa** → **prawda**

---

## `if` — podstawowa instrukcja warunkowa

Instrukcja `if` wykonuje blok kodu **tylko wtedy**, gdy warunek jest prawdziwy.

```c
if (warunek) {
    // kod
}
```

---

### `else` — kod wykonywany przy fałszu

```c
if (x > 0) {
    printf("Dodatnia\n");
} else {
    printf("Niedodatnia\n");
}
```

---

### `else if` — wiele warunków

```c
if (x == 0) {
    printf("Zero\n");
} else if (x > 0) {
    printf("Dodatnia\n");
} else {
    printf("Ujemna\n");
}
```

---

## Operator trójargumentowy `?:`

Skrócona forma `if / else`:

```c
int wynik = (x > 5) ? 1 : 0;
```

---

## `switch` — wybór wielu opcji

Instrukcja `switch` pozwala wybrać jedną z wielu gałęzi w zależności od
**wartości wyrażenia** (tzw. wyrażenia sterującego):

```c
switch (zmienna) {
    case 1:
        // ...
        break;
    case 2:
        // ...
        break;
    default:
        // ...
}
```

### Jakiego typu może być wyrażenie w `switch`?

W języku C wyrażenie w `switch` **musi mieć typ całkowity po tzw. promocji
całkowitoliczbowej**. W praktyce oznacza to, że możesz użyć:

- `int`, `short`, `long`, `long long`
- ich odmian bez znaku: `unsigned int`, `unsigned long` itd.
- `char` (np. `switch (znak) { case 'a': ... }`)
- typów wyliczeniowych `enum`

Nie możesz użyć:

- typów zmiennoprzecinkowych (`float`, `double`, `long double`)
- wskaźników (`int *`, `char *` itd.)
- struktur (`struct ...`) czy unii (`union ...`)

Dodatkowo:

- każda etykieta `case` musi być **stałą wyrażoną w czasie kompilacji** (np.
  literał liczbowy, znak, element `enum`, wyrażenie z `#define`),
- wartości w poszczególnych `case` **nie mogą się powtarzać**.

Przykład użycia `char`:

```c
char komenda = 'a';

switch (komenda) {
    case 'a':
        printf("Opcja A\n");
        break;
    case 'b':
        printf("Opcja B\n");
        break;
    default:
        printf("Nieznana komenda\n");
}
```

---

### Fall-through — co gdy nie ma `break`?

Brak `break` powoduje „wpadanie” do kolejnych case’ów:

```c
switch (x) {
    case 1:
        printf("Jeden\n");
    case 2:
        printf("Dwa\n"); // wykona się także przy x == 1!
}
```

---

### Deklaracje w case — używaj klamer

```c
switch (x) {
    case 1: {
        int a = 10;
        printf("%d\n", a);
        break;
    }
    case 2:
        printf("Case 2\n");
        break;
}
```

---

### `default`

```c
switch (opcja) {
    case 1:
        printf("Opcja 1\n");
        break;
    default:
        printf("Nieznana opcja\n");
}
```

---

### Przykład menu

```c
char opcja = '2';

switch (opcja) {
    case '1': printf("Start\n"); break;
    case '2': printf("Opcje\n"); break;
    case '3': printf("Wyjście\n"); break;
    default:  printf("Błąd\n");
}
```

---

## 📝 Quiz — sprawdź swoją wiedzę!

<QuizQuestion
    question="Która instrukcja wykona się TYLKO wtedy, gdy warunek jest prawdziwy?"
    multiple={false}
    options={[
        {
            id: 'a',
            text: 'if',
            correct: true,
            explanation:
                'if wykonuje kod tylko wtedy, gdy warunek zwróci wartość niezerową (prawdę).',
        },
        {
            id: 'b',
            text: 'else',
            correct: false,
            explanation:
                'else wykonuje się tylko wtedy, gdy poprzedni if był fałszywy — nie sprawdza warunku.',
        },
        {
            id: 'c',
            text: 'switch',
            correct: false,
            explanation:
                'switch nie sprawdza warunku logicznego, tylko dopasowuje wartość do konkretnego case.',
        },
        {
            id: 'd',
            text: 'goto',
            correct: false,
            explanation:
                'goto to skok do etykiety — nie ma nic wspólnego z warunkami.',
        },
    ]}
/>

<QuizQuestion
    question="Co się stanie, jeśli w instrukcji switch zabraknie break?"
    multiple={false}
    options={[
        {
            id: 'a',
            text: 'Program się nie skompiluje.',
            correct: false,
            explanation:
                'Brak break to nie błąd kompilacji — kod jest poprawny składniowo.',
        },
        {
            id: 'b',
            text: 'Wykona się tylko pierwszy case.',
            correct: false,
            explanation:
                'Bez break wykonają się WSZYSTKIE kolejne case-y aż do break lub końca switch.',
        },
        {
            id: 'c',
            text: 'Wykona się także kolejny case.',
            correct: true,
            explanation:
                'To tzw. fall-through — program przechodzi do następnych sekcji case.',
        },
        {
            id: 'd',
            text: 'Program się zatrzyma.',
            correct: false,
            explanation:
                'Brak break nie zatrzymuje programu — wykonanie trwa dalej.',
        },
    ]}
/>

<QuizQuestion
    question="Co robi operator trójargumentowy ?:"
    multiple={false}
    options={[
        {
            id: 'a',
            text: 'Tworzy zmienną logiczną.',
            correct: false,
            explanation:
                '?: nie tworzy zmiennej — wybiera jedną z dwóch wartości.',
        },
        {
            id: 'b',
            text: 'Zwraca jedną z dwóch wartości zależnie od warunku.',
            correct: true,
            explanation:
                'Operator ?: działa jak skrócona forma if/else dla wyrażeń.',
        },
        {
            id: 'c',
            text: 'Zastępuje switch.',
            correct: false,
            explanation:
                '?: zastępuje proste if/else, ale nie daje możliwości wielu gałęzi jak switch.',
        },
        {
            id: 'd',
            text: 'Przerywa działanie programu.',
            correct: false,
            explanation:
                'Operator ?: nie ma wpływu na przepływ programu poza wyborem wartości.',
        },
    ]}
/>

<QuizQuestion
    question="Które stwierdzenia o switch są prawdziwe?"
    multiple={true}
    options={[
        {
            id: 'a',
            text: 'default jest opcjonalny.',
            correct: true,
            explanation:
                'default nie jest wymagany — ale warto go dodawać jako obsługę nieprzewidzianych przypadków.',
        },
        {
            id: 'b',
            text: 'Wyrażenie w switch po promocji musi mieć typ całkowity (np. int, char, enum).',
            correct: true,
            explanation:
                'switch akceptuje tylko typy całkowitoliczbowe oraz enum — nie działa z float, double itd.',
        },
        {
            id: 'c',
            text: 'Można mieć dwa identyczne case-y.',
            correct: false,
            explanation:
                'Powtarzające się wartości case powodują błąd kompilacji — etykieta musi być unikalna.',
        },
        {
            id: 'd',
            text: 'switch może zastąpić wiele if-else.',
            correct: true,
            explanation:
                'switch często upraszcza kod z wieloma porównaniami do tej samej zmiennej.',
        },
    ]}
/>

<QuizQuestion
    question="Co zwróci operator (x > 5) ? 1 : 2 dla x = 3?"
    multiple={false}
    options={[
        {
            id: 'a',
            text: '1',
            correct: false,
            explanation:
                '1 zwróciłby, gdyby warunek był prawdziwy — ale 3 > 5 jest fałszem.',
        },
        {
            id: 'b',
            text: '2',
            correct: true,
            explanation:
                'Warunek (x > 5) jest fałszywy, więc zwracana jest wartość po dwukropku: 2.',
        },
        {
            id: 'c',
            text: '3',
            correct: false,
            explanation:
                'Operator ?: nie zwraca wartości zmiennej x, tylko jedną z dwóch zdefiniowanych opcji.',
        },
        {
            id: 'd',
            text: 'Błąd kompilacji',
            correct: false,
            explanation:
                'Wyrażenie jest poprawne — nie ma żadnego błędu składni.',
        },
    ]}
/>

```c
int x = 2;
switch (x) {
    case 1:
    case 2 + 0:
        printf("A\n");
        break;
    case 2:
        printf("B\n");
        break;
    default:
        printf("C\n");
}
```

<QuizQuestion
    question="Co wypisze powyższy kod?"
    multiple={false}
    options={[
        {
            id: 'a',
            text: 'Wypisze A',
            correct: false,
            explanation:
                'Wartość 2 pasowałaby do case 2, ale case 2 + 0 ma tę samą wartość i powoduje konflikt.',
        },
        {
            id: 'b',
            text: 'Wypisze B',
            correct: false,
            explanation:
                'Nie dojdzie do wykonania kodu — program nie przejdzie kompilacji.',
        },
        {
            id: 'c',
            text: 'Wypisze C',
            correct: false,
            explanation:
                'Również nie, bo switch nie wykona się z powodu błędu kompilacji.',
        },
        {
            id: 'd',
            text: 'Kod się nie skompiluje',
            correct: true,
            explanation:
                'case 2 i case 2 + 0 mają identyczną wartość — a powtarzające się etykiety są błędem kompilacji.',
        },
    ]}
/>
