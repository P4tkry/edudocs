/** @type {import("prettier").Config} */
const config = {
    printWidth: 80,
    tabWidth: 4,
    semi: true,
    singleQuote: true,
    trailingComma: 'all',
    bracketSpacing: true,
    arrowParens: 'always',
    endOfLine: 'lf',
    proseWrap: 'always',
};

export default config;
